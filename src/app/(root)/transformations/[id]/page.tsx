import React from "react";

type TransformationPageProps = {
	params: {
		id: string
	}
};

const TransformationPage = ({params:{id}}: TransformationPageProps) => {
	return <div>TransFormation Page</div>;
};

export default TransformationPage;